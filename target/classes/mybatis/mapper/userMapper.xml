<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.csy.test.mapper.UserMapper" >

    <!--添加用户信息-->
    <insert id="save" parameterType="com.csy.test.entity.User">
        insert into user(id,name,password) values(#{id},#{name},#{age})
    </insert>
</mapper>